import { FinnishDecimalPipe } from './finnish-decimal.pipe';

describe('FinnishDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new FinnishDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
